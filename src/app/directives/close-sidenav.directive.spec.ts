import { CloseSidenavDirective } from './close-sidenav.directive';

describe('CloseSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseSidenavDirective();
    expect(directive).toBeTruthy();
  });
});
